export default function sayHello(name) {
  return `Hello ${name}`
}

export const add = (a, b) => a + b
export const substract = (a, b) => a - b

// export const sayHello2 = (name) => `Hello ${name}`

// export default sayHello2